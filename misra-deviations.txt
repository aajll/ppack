# Project-level MISRA deviations for ppack. Each entry is preceded by the
# rationale that misch harvests into the deviation record. A bare rule id
# deviates that rule across every in-scope file; add :file or :file:line to
# narrow the scope.

# Rule 15.5 (a function should have a single point of exit) is deviated
# project-wide as a deliberate house style. Precondition guard clauses that
# return early on invalid input and the branch-per-outcome returns in the
# pack/unpack type-dispatch switches are clearer and less error-prone than
# threading a result variable through nested if/else. None of these
# functions acquire a resource, take a lock, or have any teardown, so there
# is no shared cleanup that a single exit point would protect. Advisory;
# deviation accepted.
misra-c2012-15.5
